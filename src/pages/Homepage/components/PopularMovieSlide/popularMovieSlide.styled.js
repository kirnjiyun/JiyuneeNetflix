import styled from "styled-components";

export const CarouselContainer = styled.div`
    .carousel-container {
        overflow: hidden;
        &:hover {
            overflow: visible;
        }
    }
`;
